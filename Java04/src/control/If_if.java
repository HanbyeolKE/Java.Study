package control;

public class If_if {
	public static void main(String[] args) {
		// 제어문은 중첨해서 사용할 수 있음
		// if 안에 if, if 안에 for, for 안에 if, for 안에 for
		int i = 0;
		int j = 0;

		// if 문 안에 if문을 사용할 수 있음
		if (i < 0) {
			// 0 보다 작으면
		} else if (i == 0) {
			// 0 보다 같으면
		} else {
			// 0 보다 크면
		}
	}
}

